import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.scaningcapasitorapp',
  appName: 'Scaning-Capacitor-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
